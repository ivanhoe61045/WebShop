@model IEnumerable<WebShopSite.Models.WebShopViewModels.GenderViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<label class="alert-danger">@ViewBag.ErrorMessage</label>
<label class="alert-success">@ViewBag.Message</label>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdGender)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GenderName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdGender)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GenderName)
        </td>
        <td>
            
        </td>
    </tr>
}

</table>
